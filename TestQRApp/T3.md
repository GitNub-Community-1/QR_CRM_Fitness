 # ПОЛНОЕ ТЕХНИЧЕСКОЕ ЗАДАНИЕ (ТЗ)
## Проект: Система контроля доступа «FitPass» (.NET 9 + Blazor + PostgreSQL)
### 1. 🗄️ Структура БД и Entity-модели (EF Core)
#### 1.1. Сущность Тренера (Trainer.cs)
``` csharp

public class Trainer
{
    public Guid Id { get; set; } = Guid.NewGuid();
    public string FirstName { get; set; } = string.Empty;
    public string LastName { get; set; } = string.Empty;
    public string Specialization { get; set; } = string.Empty;

    // Навигационное свойство: у одного тренера может быть много клиентов
    public ICollection<Client> Clients { get; set; } = new List<Client>();
}
```
#### 1.2. Сущность Клиента (Client.cs)
```csharp

public class Client
{
    public Guid Id { get; set; } = Guid.NewGuid();
    public string FirstName { get; set; } = string.Empty;
    public string LastName { get; set; } = string.Empty;
    public string? MiddleName { get; set; }
    public int Age { get; set; }
    public Gender Gender { get; set; }
    public string Login { get; set; } = string.Empty; // Номер телефона
    public string PasswordHash { get; set; } = string.Empty;
    
    // Ограничение «одно устройство»
    public string? ActiveDeviceId { get; set; }
    public DateTimeOffset CreatedAt { get; set; } = DateTimeOffset.UtcNow;

    // Связь с тренером (Один-ко-многим, Nullable)
    public Guid? TrainerId { get; set; }
    public Trainer? Trainer { get; set; }

    // Навигационные свойства
    public ICollection<UserSubscription> Subscriptions { get; set; } = new List<UserSubscription>();
    public ICollection<AccessLog> AccessLogs { get; set; } = new List<AccessLog>();
}

public enum Gender
{
    Male,
    Female
}
```
#### 1.3. Сущность Тарифа (SubscriptionType.cs)
```csharp

public class SubscriptionType
{
    public int Id { get; set; }
    public string Name { get; set; } = string.Empty; // например, "Женский Безлимит"
    public int DurationInDays { get; set; }
    public decimal Price { get; set; }

    public ICollection<UserSubscription> UserSubscriptions { get; set; } = new List<UserSubscription>();
}
```
#### 1.4. Сущность Абонемента Клиента (UserSubscription.cs)
```csharp

public class UserSubscription
{
    public Guid Id { get; set; } = Guid.NewGuid();

    // Связь с Клиентом (Один-ко-многим: у клиента может быть история из нескольких абонементов)
    public Guid ClientId { get; set; }
    public Client Client { get; set; } = null!;

    // Связь с Тарифом
    public int SubscriptionTypeId { get; set; }
    public SubscriptionType SubscriptionType { get; set; } = null!;

    public DateTimeOffset StartDate { get; set; }
    public DateTimeOffset EndDate { get; set; }
    public SubscriptionStatus Status { get; set; } = SubscriptionStatus.Active;
}

public enum SubscriptionStatus
{
    Active,
    Expired,
    Suspended
}
```
#### 1.5. Сущность Персонала (Staff.cs)
```csharp

public class Staff
{
    public Guid Id { get; set; } = Guid.NewGuid();
    public string FirstName { get; set; } = string.Empty;
    public string LastName { get; set; } = string.Empty;
    public string Login { get; set; } = string.Empty;
    public string PasswordHash { get; set; } = string.Empty;
    public StaffRole Role { get; set; }
}

public enum StaffRole
{
    Admin,
    Moderator // Ресепшн
}
```
#### 1.6. Сущность Лога Доступа (AccessLog.cs)
```csharp

public class AccessLog
{
    public Guid Id { get; set; } = Guid.NewGuid();
    
    // Связь с клиентом
    public Guid ClientId { get; set; }
    public Client Client { get; set; } = null!;

    public DateTimeOffset ScanTime { get; set; } = DateTimeOffset.UtcNow;
    public bool IsSuccess { get; set; }
    public string? FailReason { get; set; }
}
```
### 2. 📚 Полный каталог Интерфейсов и Сервисов
#### 2.1. Авторизация и Сессии (IAuthService)
```csharp

public interface IAuthService
{
    // Вход для персонала (Админ/Модератор)
    Task<AuthResultDto> LoginStaffAsync(string login, string password);

    // Вход для клиента (с привязкой к девайсу)
    Task<ClientAuthResultDto> LoginClientAsync(string login, string password, string deviceId);

    // Сброс привязанного устройства (вызывает только Админ/Модератор)
    Task<bool> ResetClientDeviceAsync(Guid clientId);

    // Выход из системы (очистка Cookie)
    Task LogoutAsync();
}

public record AuthResultDto(bool IsSuccess, string ErrorMessage, StaffRole? Role, string? FirstName);

public record ClientAuthResultDto(bool IsSuccess, string ErrorMessage, Guid? ClientId);
```
#### 2.2. Сервис QR-кодов (IQrCodeService)
```csharp

public interface IQrCodeService
{
    // Генерирует шифрованную строку-токен (внутри: ClientId + DeviceId + Время генерации)
    string GenerateQrToken(Guid clientId, string deviceId);

    // Превращает строку-токен в Base64 картинку для тега <img src="..." />
    string GenerateQrImageBase64(string qrToken);

    // Дешифрует и проверяет токен. Если он валидный — возвращает ClientId
    Task<QrValidationResultDto> ValidateQrTokenAsync(string qrToken);
}

public record QrValidationResultDto(bool IsValid, string ErrorMessage, Guid? ClientId);
```
#### 2.3. Контроль доступа на ресепшене (IAccessControlService)
```csharp

public interface IAccessControlService
{
    // Главный метод: обрабатывает сканирование QR на ресепшене
    Task<AccessResultDto> ProcessEntryAsync(string qrToken);
}

public record AccessResultDto(
    bool IsSuccess,
    string Message,
    string? FirstName,
    string? LastName,
    int? Age,
    string? Gender,
    string? TrainerName,
    string? SubscriptionTypeName,
    DateTimeOffset? EndDate);
```
#### 2.4. Аналитика для Главной Панели (IAnalyticsService)
```csharp

public interface IAnalyticsService
{
    // Сколько людей зашло за последние 3 часа
    Task<int> GetCurrentGymLoadAsync();

    // Всего успешных посещений за сегодня
    Task<int> GetTodayVisitsCountAsync();

    // Новых регистраций за текущий месяц
    Task<int> GetMonthlyNewClientsCountAsync();

    // Последние N входов для живой ленты на ресепшене
    Task<List<RecentPassDto>> GetRecentPassesAsync(int count = 10);
}

public record RecentPassDto(string ClientFullName, DateTimeOffset ScanTime, bool IsSuccess, string? FailReason);
```
#### 2.5. CRUD-сервис Тренеров (ITrainerService)
```csharp

public interface ITrainerService
{
    Task<Guid> CreateTrainerAsync(CreateTrainerDto dto);
    Task<TrainerDto?> GetTrainerByIdAsync(Guid id);
    Task<List<TrainerDto>> GetAllTrainersAsync();
    Task<bool> UpdateTrainerAsync(Guid id, CreateTrainerDto dto);
    Task<bool> DeleteTrainerAsync(Guid id);
}

public record CreateTrainerDto(string FirstName, string LastName, string Specialization);

public record TrainerDto(Guid Id, string FullName, string Specialization);
```
#### 2.6. CRUD-сервис Тарифов Абонементов (ISubscriptionTypeService)
```csharp

public interface ISubscriptionTypeService
{
    Task<int> CreateTypeAsync(CreateSubscriptionTypeDto dto);
    Task<List<SubscriptionTypeDto>> GetAllTypesAsync();
    Task<bool> UpdateTypeAsync(int id, CreateSubscriptionTypeDto dto);
    Task<bool> DeleteTypeAsync(int id);
}

public record CreateSubscriptionTypeDto(string Name, int DurationInDays, decimal Price);

public record SubscriptionTypeDto(int Id, string Name, int DurationInDays, decimal Price);
```
#### 2.7. Управление Клиентами и Абонементами (IClientManagerService)
```csharp

public interface IClientManagerService
{
    // Регистрация клиента модератором
    Task<Guid> RegisterClientAsync(CreateClientDto dto);

    // Привязка/продление абонемента
    Task<bool> AssignSubscriptionAsync(Guid clientId, int subscriptionTypeId);

    // Список клиентов с поиском и пагинацией для административной панели
    Task<PagedListDto<ClientListDto>> GetClientsAsync(string searchTerm, int page, int pageSize);

    // Детальная информация по клиенту
    Task<ClientDetailDto?> GetClientDetailAsync(Guid clientId);
}

public record CreateClientDto(
    string FirstName, 
    string LastName, 
    string? MiddleName, 
    int Age, 
    Gender Gender, 
    string Login, 
    string Password, 
    Guid? TrainerId);

public record ClientListDto(Guid Id, string FullName, string Login, string? ActiveSubscriptionName, string Status);

public record ClientDetailDto(
    Guid Id, 
    string FullName, 
    int Age, 
    string Gender, 
    string? TrainerName, 
    string? ActiveSubscriptionName, 
    DateTimeOffset? SubscriptionEndDate, 
    string? DeviceId);

public record PagedListDto<T>(List<T> Items, int TotalCount, int Page, int PageSize);
```
#### 2.8. Управление сотрудниками (Только для супер-админа) (IStaffService)
```csharp

public interface IStaffService
{
    Task<Guid> CreateStaffAsync(CreateStaffDto dto);
    Task<List<StaffDto>> GetAllStaffAsync();
    Task<bool> DeleteStaffAsync(Guid id);
}

public record CreateStaffDto(string FirstName, string LastName, string Login, string Password, StaffRole Role);

public record StaffDto(Guid Id, string FullName, string Login, StaffRole Role);
```
### 3. ⚙️ Регистрация в DI-контейнере (Program.cs)
```csharp

var builder = WebApplication.CreateBuilder(args);

// 1. Подключение PostgreSQL через EF Core
builder.Services.AddDbContext<AppDbContext>(options =>
    options.UseNpgsql(builder.Configuration.GetConnectionString("DefaultConnection")));

// 2. Настройка Cookie-авторизации
builder.Services.AddAuthentication(CookieAuthenticationDefaults.AuthenticationScheme)
    .AddCookie(options =>
    {
        options.Cookie.Name = "FitPass.Auth";
        options.Cookie.HttpOnly = true; // Защита от XSS
        options.Cookie.SecurePolicy = CookieSecurePolicy.Always; // Только HTTPS
        options.Cookie.SameSite = SameSiteMode.Strict; // Защита от CSRF
        options.LoginPath = "/login";
    });

// 3. Регистрация бизнес-сервисов
builder.Services.AddScoped<IAuthService, AuthService>();
builder.Services.AddScoped<IQrCodeService, QrCodeService>();
builder.Services.AddScoped<IAccessControlService, AccessControlService>();
builder.Services.AddScoped<IAnalyticsService, AnalyticsService>();
builder.Services.AddScoped<ITrainerService, TrainerService>();
builder.Services.AddScoped<ISubscriptionTypeService, SubscriptionTypeService>();
builder.Services.AddScoped<IClientManagerService, ClientManagerService>();
builder.Services.AddScoped<IStaffService, StaffService>();

var app = builder.Build();
```
🎯 Резюме

С таким ТЗ ты защищен от путаницы. Каждый интерфейс решает свою изолированную задачу, а связи в базе данных настроены максимально правильно и безопасно. Ты можешь брать этот файл за основу и писать код реализации!
